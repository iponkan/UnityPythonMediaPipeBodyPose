[project]
name = "mediapipebody"
version = "0.1.0"
description = "Unity MediaPipe body pose pipe server"
requires-python = ">=3.10,<3.11"
dependencies = [
    "mediapipe==0.10.5",
]

[tool.uv]
package = false
